hey we're going to try and adopt speckit for this project, i installed a fresh copy already, first step: writing a constitution. The docs about it say:

```
2. Establish project principles
Launch your AI assistant in the project directory. The /speckit.* commands are available in the assistant.

Use the /speckit.constitution command to create your project's governing principles and development guidelines that will guide all subsequent development.

/speckit.constitution Create principles focused on code quality, testing standards, user experience consistency, and performance requirements
```

Given that the aforementioned example is rather generic but does show us at least the type of prompt and phrasing we need to pass into  the constitution command, i need your help priming the input for it based on the project we're working in so help me interactively refine the primed prompt we're going to toss into it. Can you help?

i am aware that when we give an input shaped like literal princeples already the agent is inclined to just copy-paste them without using it's full potential and power and does not look at the codebas in any way, so please watch that when phrasing asd for your questions: